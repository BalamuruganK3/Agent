# Agent
its to complete the AI Agents
Step 1: open the folder in the VS code
Step 2: Open the terminal 
Step 3: run command (to create virtual environment)
python -m venv venv
Step 4: run command (to install dependences)
pip install -r requirements.txt
pip install streamlit
step 5: run command (to activate the Virtual environment)
.\venv\Scripts\activate 
(Check your terminal is in Tech\workflow-optimizer -If not run cd Workflow-optimizer)
step 6: run command ( to execute the code)
streamlit run main.py
